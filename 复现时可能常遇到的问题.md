### 问题一
'getBaseMapper()' in 'com.baomidou.mybatisplus.extension.service.impl.ServiceImpl' clashes with 'getBaseMapper()' in 'com.baomidou.mybatisplus.extension.service.IService'; incompatible return type
Type parameter 'top.aichezhan.ssyx.mapper.CategoryMapper' is not within its bound; should extend 'com.baomidou.mybatisplus.core.mapper.BaseMapper<top.aichezhan.ssyx.model.product.Category>'
##### 解答
问题出现在顺序问题，
顺序为Controller->Service->Mapper时，当我们在处理Service会出现以上报错，原因是Mapper层还没有extend BaseMapper
如果顺序为Mapper->Service->Controller就不会出现这个问题

一、 架构优化：使用 MinIO 替换阿里云 OSS
在微服务项目中，为了降低云服务成本并掌握底层技术，决定在 Ubuntu 22.04 服务器上使用 Docker 部署开源的 MinIO 作为分布式文件存储服务，平替原有的阿里云 OSS。

1. 核心修改点总结
   依赖替换 (pom.xml)：移除 aliyun-sdk-oss，引入 io.minio:minio:8.4.3。

配置修改 (application-dev.yml)：

YAML
minio:
endpoint: http://<服务器公网IP>:9000  # API通讯端口
accessKey: minioadmin
secretKey: minioadmin
bucketName: ssyx-bucket
代码重写：重写 FileUploadServiceImpl，使用 MinioClient 的 putObject 方法实现文件流上传，并保留了原有的 UUID 防重名和日期分目录逻辑。

权限注意：在 MinIO 管理后台（9001 端口）创建 Bucket 后，必须将 Access Policy 修改为 Public，否则前端无法回显图片。

二、 环境部署踩坑与排查 (Docker & 网络)
坑点 1：Docker 拉取镜像报 context deadline exceeded
问题描述：在 Ubuntu 上执行 docker run 下载 MinIO 镜像时，长时间卡顿后报错超时。

根本原因：国内服务器访问 Docker Hub 官方仓库网络受限。

解决方案：为 Docker 配置国内镜像加速器（推荐使用阿里云专属加速器）。

修改 /etc/docker/daemon.json 写入 registry-mirrors。

执行 sudo systemctl daemon-reload 和 sudo systemctl restart docker 重启生效。

坑点 2：MinIO 容器内部正常，但外部浏览器“无法访问此页面”
排查过程：

使用 curl -I http://localhost:9001 在服务器内部测试，返回 200 OK，确认容器运行正常。

检查阿里云安全组，已放行 9000 和 9001 端口。

检查 Ubuntu 底层防火墙 sudo ufw status，发现并未放行相关端口。

根本原因：服务器存在双重防火墙（阿里云安全组 + 宝塔面板/UFW 系统防火墙），且**代理软件（梯子）**可能导致本地网络路由异常。

最终解决：

关闭本地代理软件，使用浏览器无痕模式访问，避免自动跳转 HTTPS。

在宝塔面板的“安全”菜单中，手动添加并放行 9000 和 9001 端口（宝塔会接管底层 UFW 防火墙）。

三、 Spring Boot 业务代码踩坑 (Bean 注入问题)
在完善商品服务（service-product）时，IDEA 出现红色报错。

坑点 3：Cannot resolve symbol 'skuInfoMapper'
问题描述：在 SkuInfoServiceImpl 中使用 skuInfoMapper 变量时提示未定义。

根本原因：虽然继承了 ServiceImpl，但如果代码中显式调用了 Mapper 实例，需要手动注入。

解决方案：在类中使用 @Autowired 显式注入 SkuInfoMapper 即可。

坑点 4：Could not autowire. No beans of 'XxxService' type found.
问题描述：注入 SkuImagesService、SkuPosterService 等 Service 时报错找不到 Bean。

根本原因：教程文档中部分基础的增删改查代码被省略（标记为 //TODO），导致这些对应的 Mapper、Service 接口及实现类根本没有创建；或者创建了实现类但忘记加 @Service 注解。

解决方案：

老老实实补全 SkuImage、SkuPoster、SkuAttrValue 对应的 Mapper 接口、Service 接口。

最关键的一步：在对应的 ServiceImpl 实现类上加上 @Service 注解，将其交由 Spring 容器管理。